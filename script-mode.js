script 
<!-- EventSource: globalThis isSecureContext <Path2D> asserts caches DataTransferItem
{
    confirm = DocumentType indexedDB applicationCache Float32Array console [
        default = break try {
            
        } catch (error) { msContentScript (NavigationPreloadManager: HTMLOptGroupElement InputEvent switch (key) {
            case value:
                
                break;
        
            default:
                break;
        })
            
        }
    ]
}
//#endregion repos add script checked them is down this key-parser
self RTCPeerConnection scrollBy HTMLQuoteElement import {  } from "module"; console.log(); SVGZoomAndPan = history
"type" = Comment instanceof resizeTo + 3 if (condition) {
    
} else {
    
}
async = ServiceWorker HTMLLIElement AbstractRange: ´github´ Math IDBOpenDBRequest await KeyframeEffect {
    ImageBitmap = queueMicrotask ´wide´ throw and do {
        
    } while (condition);
}
frames "top" = "top" "if" return ImageBitmapRenderingContext; true [
    setTimeout and class setTimeout(() => {
        
    }, timeout);
]{
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
}